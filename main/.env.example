# 이 파일은 예시입니다. 실제 값을 채운 뒤 파일명을 .env로 저장하고
# 로컬 환경변수(또는 IDE의 Run Configuration, direnv 등)로 주입해 사용하세요.
# .env 파일 자체는 Git에 커밋되지 않습니다.

# ── 활성 프로필 (local | prod) ──────────────────────────
SPRING_PROFILES_ACTIVE=local

# ── Supabase PostgreSQL 접속 정보 (Phase 1) ────────────
# Supabase 프로젝트 설정 > Database > Connection Info 에서 확인
DB_HOST=
DB_PORT=5432
DB_NAME=postgres
DB_USERNAME=
DB_PASSWORD=

# ── JWT 인증 (Phase 4에서 사용 예정) ────────────────────
JWT_SECRET=
JWT_ACCESS_TOKEN_EXPIRATION=
JWT_REFRESH_TOKEN_EXPIRATION=

# ── Supabase Storage (Phase 6에서 사용 예정) ────────────
SUPABASE_STORAGE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_STORAGE_BUCKET=

# ── OpenAI API (Phase 10에서 사용 예정) ─────────────────
OPENAI_API_KEY=
