# 이 파일은 예시입니다. 실제 값을 채운 뒤 파일명을 .env로 저장하세요.
# .env 파일 자체는 Git에 커밋되지 않습니다.

# 백엔드(main/) API 서버 주소. 기본값(코드에 내장된 값)은 배포된 서버 주소입니다.
# 로컬에서 직접 띄운 백엔드(8080 포트)를 바라보게 하려면 아래처럼 오버라이드하세요.
VITE_API_BASE_URL=http://localhost:8080/api
# VITE_API_BASE_URL=https://api.madmon.madcamp-kaist.org/api
